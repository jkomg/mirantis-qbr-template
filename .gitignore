# ---- Editor & OS ----
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp
*~

# ---- Build output ----
node_modules/
dist/
build/

# ---- Container ----
# Fonts are fetched at docker build time — don't commit them
assets/fonts/*.woff2
!assets/fonts/.gitkeep

# ---- Customer data — NEVER commit ----
# Per-account QBR data files. Real customer ARR, ticket counts, etc. must
# live in a private store (not git). Keep this aggressive — easier to
# undo a false negative than to scrub a leaked secret.
accounts/
*.qbr.data.json
qbr.data.local.json
draft.json

# ---- Kubernetes ----
# Manifests are committed, but never one with inlined credentials. Secrets are
# created at deploy time and stay out of the repo:
#   kubectl create secret generic mirantis-qbr-sf-sync-env --from-env-file=.env
k8s/*secret*.yaml
k8s/*.local.yaml

# ---- Project working folders ----
uploads/
screenshots/
extracted/
scraps/
exports/

# ---- Local dev ----
.env
.env.local
*.log
.aider*
